# Local vendor directory

MinifyCode does not load third-party code from a CDN. The current browser runtime is dependency-free, so this directory is intentionally reserved for future vendored libraries such as a local Terser build.
